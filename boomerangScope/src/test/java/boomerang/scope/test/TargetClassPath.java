package boomerang.scope.test;

import java.io.File;

public class TargetClassPath {

    public static final String TARGET_CLASS_PATH = System.getProperty("user.dir") + File.separator + ".." + File.separator + "boomerangScope" + File.separator + "target" + File.separator + "test-classes";
}
